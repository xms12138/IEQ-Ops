"""IEQ-Bench — offline evaluation harness for IEQ-Ops (Phase 4).

No claim without numbers (CLAUDE.md Principle #8). Tasks are capability-addressed
and drive the REAL system entries (planner / specialist subgraph / critic /
retrieval / end-to-end graph) — never a parallel mock. See eval/runner.py.
"""
