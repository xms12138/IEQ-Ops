The query below was used to retrieve indoor-environment standard excerpts, but the result was judged insufficient.

Original query:
$query

What was missing:
$reason

Rewrite ONE new query that is more likely to retrieve the missing information. Output only the new query on a single line — no explanation, no quotes, no prefix.
